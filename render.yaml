services:
  - type: web
    name: selfcompass
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: CLAUDE_API_KEY
        sync: false
      - key: CLAUDE_MODEL
        value: claude-sonnet-4-20250514